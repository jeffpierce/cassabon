dependencies:
  override:
    - make fetch
    - rm -rf ~/.go_workspace/src/github.com/change/cassabon
    - ln -sf ~/cassabon ~/.go_workspace/src/github.com/change/cassabon
    - make clean build

test:
  override:
    - make test
